# Demo

The final demo must show a real Logos Storage upload, a `storageUploadDone` CID,
Delivery broadcast discovery on `/lp0017-whistleblower/1/cids/json`, batch anchor,
and registry query confirmation with `RISC0_DEV_MODE=0` visible in terminal output.
