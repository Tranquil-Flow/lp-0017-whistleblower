# Whistleblower

LP-0017 Whistleblower bounty implementation: a Logos Basecamp app plus reusable
document-indexing module for upload -> broadcast -> anchor flows.

Status: early scaffold + adapter-boundary spike. The reusable Rust crates are
kept Qt-free; Qt/Basecamp plugin ownership belongs in `adapters/qt` and `ui`.
